// Activating Strict Moder in java script.
'use strict';
// How stict mode works

let hasDriversLicense = false;
const passTest= true;
if (passTest) hasDriverLicense=true; // In this line i forgest s in hasDriver .
// if i run it without strict mode it didn't show me the error in console.
if (hasDriversLicense) console.log('I can Drive')